import Ship from "./Ship";

const Gameboard = () => {
  // take info, and create a ship with length and add it to ships obj, and return whether or not it was successful.
  const placeShip = (xCord, yCord, dir, len) => {};

  return { placeShip };
};

export { locationTaken, Gameboard };
